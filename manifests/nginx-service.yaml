apiVersion: v1
kind: Service
metadata:
  name: nginx-observability-demo
spec:
  selector:
    app: nginx-observability-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
